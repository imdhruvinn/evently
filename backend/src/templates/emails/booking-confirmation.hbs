<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Booking Confirmed</title>
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
      sans-serif; margin: 0; padding: 0; background-color: #f8fafc; } .container
      { max-width: 600px; margin: 0 auto; background: white; } .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); padding:
      40px 20px; text-align: center; } .header h1 { color: white; margin: 0;
      font-size: 28px; font-weight: 700; } .content { padding: 40px 30px; }
      .ticket-card { background: #f8fafc; border-radius: 12px; padding: 30px;
      margin: 25px 0; border-left: 4px solid #10b981; } .event-name { font-size:
      24px; font-weight: 700; color: #1f2937; margin-bottom: 15px; }
      .event-details { display: grid; grid-template-columns: 1fr 1fr; gap: 20px;
      margin: 20px 0; } .detail { display: flex; align-items: center; }
      .detail-icon { width: 20px; height: 20px; margin-right: 10px; }
      .detail-text { color: #4b5563; font-weight: 500; } .booking-summary {
      background: white; border: 2px solid #e5e7eb; border-radius: 8px; padding:
      20px; margin: 25px 0; } .summary-row { display: flex; justify-content:
      space-between; margin: 10px 0; } .summary-total { font-weight: 700;
      font-size: 18px; color: #1f2937; border-top: 2px solid #e5e7eb;
      padding-top: 15px; margin-top: 15px; } .cta-button { display:
      inline-block; background: #10b981; color: white; padding: 15px 30px;
      border-radius: 8px; text-decoration: none; font-weight: 600; margin: 20px
      0; } .footer { background: #f9fafb; padding: 30px; text-align: center;
      color: #6b7280; font-size: 14px; } .qr-placeholder { width: 120px; height:
      120px; background: #e5e7eb; border-radius: 8px; margin: 0 auto; display:
      flex; align-items: center; justify-content: center; color: #9ca3af; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>🎉 Booking Confirmed!</h1>
        <p
          style="color: rgba(255,255,255,0.9); margin: 10px 0 0 0; font-size: 16px;"
        >Your ticket is ready, {{userName}}!</p>
      </div>

      <div class="content">
        <div class="ticket-card">
          <div class="event-name">{{eventName}}</div>

          <div class="event-details">
            <div class="detail">
              <span class="detail-icon">📍</span>
              <span class="detail-text">{{venue}}</span>
            </div>
            <div class="detail">
              <span class="detail-icon">📅</span>
              <span class="detail-text">{{eventDate}}</span>
            </div>
            <div class="detail">
              <span class="detail-icon">⏰</span>
              <span class="detail-text">{{eventTime}}</span>
            </div>
            <div class="detail">
              <span class="detail-icon">🎫</span>
              <span class="detail-text">{{quantity}}
                {{#if (eq quantity 1)}}Ticket{{else}}Tickets{{/if}}</span>
            </div>
          </div>

          <div class="qr-placeholder">
            {{#if qrCodeData}}
              <img
                src="{{qrCodeData}}"
                alt="QR Code"
                style="width: 100px; height: 100px; display: block; margin: 0 auto;"
              />
            {{else}}
              <span>QR Code</span>
            {{/if}}
          </div>
        </div>

        <div class="booking-summary">
          <h3 style="margin-top: 0; color: #1f2937;">Booking Summary</h3>
          <div class="summary-row">
            <span>Booking ID:</span>
            <span
              style="font-family: monospace; background: #f3f4f6; padding: 2px 6px; border-radius: 4px;"
            >{{bookingId}}</span>
          </div>
          <div class="summary-row">
            <span>Tickets ({{quantity}}x):</span>
            <span>${{totalPrice}}</span>
          </div>
          <div class="summary-row summary-total">
            <span>Total Paid:</span>
            <span>${{totalPrice}}</span>
          </div>
        </div>

        <div style="text-align: center;">
          <a href="#" class="cta-button">View Full Ticket</a>
        </div>

        <div
          style="background: #fffbeb; border: 1px solid #fbbf24; border-radius: 8px; padding: 20px; margin: 30px 0;"
        >
          <h4 style="color: #92400e; margin-top: 0;">📱 Important Reminders</h4>
          <ul style="color: #92400e; margin: 0; padding-left: 20px;">
            <li>Save this email - you'll need it for entry</li>
            <li>Arrive 30 minutes before the event starts</li>
            <li>Bring a valid photo ID</li>
            <li>Screenshots of tickets are not accepted</li>
          </ul>
        </div>
      </div>

      <div class="footer">
        <p><strong>Questions?</strong> Contact us at support@evently.com</p>
        <p>© 2025 Evently. All rights reserved.</p>
        <p style="font-size: 12px; margin-top: 20px;">
          If you have any issues with your booking, please contact our support
          team immediately.
        </p>
      </div>
    </div>
  </body>
</html>