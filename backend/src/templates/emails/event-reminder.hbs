<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Event Reminder</title>
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
      sans-serif; margin: 0; padding: 0; background-color: #f8fafc; } .container
      { max-width: 600px; margin: 0 auto; background: white; } .header {
      background: linear-gradient(135deg, #f59e0b 0%, #ef4444 100%); padding:
      40px 20px; text-align: center; } .header h1 { color: white; margin: 0;
      font-size: 28px; font-weight: 700; } .content { padding: 40px 30px; }
      .countdown-card { background: linear-gradient(135deg, #fef3c7 0%, #fed7aa
      100%); border-radius: 12px; padding: 30px; margin: 25px 0; text-align:
      center; } .countdown-number { font-size: 48px; font-weight: 900; color:
      #92400e; margin: 0; } .countdown-text { color: #92400e; font-size: 18px;
      font-weight: 600; margin: 5px 0; } .event-details { background: #f8fafc;
      border-radius: 12px; padding: 25px; margin: 25px 0; } .event-name {
      font-size: 24px; font-weight: 700; color: #1f2937; margin-bottom: 20px; }
      .detail-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 15px; }
      .detail { display: flex; align-items: center; margin: 10px 0; }
      .detail-icon { margin-right: 10px; font-size: 18px; } .detail-text {
      color: #4b5563; font-weight: 500; } .cta-button { display: inline-block;
      background: #ef4444; color: white; padding: 15px 30px; border-radius: 8px;
      text-decoration: none; font-weight: 600; margin: 20px 0; } .footer {
      background: #f9fafb; padding: 30px; text-align: center; color: #6b7280;
      font-size: 14px; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>⏰ Event Reminder</h1>
        <p
          style="color: rgba(255,255,255,0.9); margin: 10px 0 0 0; font-size: 16px;"
        >Don't miss out, {{userName}}!</p>
      </div>

      <div class="content">
        <div class="countdown-card">
          <div class="countdown-number">{{hoursUntilEvent}}</div>
          <div class="countdown-text">
            {{#if (eq hoursUntilEvent 1)}}
              Hour to go!
            {{else if (lt hoursUntilEvent 24)}}
              Hours to go!
            {{else}}
              Hours until your event!
            {{/if}}
          </div>
        </div>

        <div class="event-details">
          <div class="event-name">{{eventName}}</div>

          <div class="detail-grid">
            <div class="detail">
              <span class="detail-icon">📍</span>
              <span class="detail-text">{{venue}}</span>
            </div>
            <div class="detail">
              <span class="detail-icon">📅</span>
              <span class="detail-text">{{eventDate}}</span>
            </div>
            <div class="detail">
              <span class="detail-icon">⏰</span>
              <span class="detail-text">{{eventTime}}</span>
            </div>
            <div class="detail">
              <span class="detail-icon">🎫</span>
              <span class="detail-text">Your Tickets</span>
            </div>
          </div>
        </div>

        <div style="text-align: center;">
          <a href="#" class="cta-button">View Your Tickets</a>
        </div>

        <div
          style="background: #fef2f2; border: 1px solid #fca5a5; border-radius: 8px; padding: 25px; margin: 30px 0;"
        >
          <h4 style="color: #dc2626; margin-top: 0;">🚨 Last-Minute Checklist</h4>
          <div
            style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px; color: #dc2626;"
          >
            <div>✅ Save your tickets offline</div>
            <div>✅ Check traffic/transport</div>
            <div>✅ Bring valid photo ID</div>
            <div>✅ Arrive 30 mins early</div>
            <div>✅ Check weather forecast</div>
            <div>✅ Charge your phone</div>
          </div>
        </div>

        {{#if (lt hoursUntilEvent 12)}}
          <div
            style="background: #f0fdf4; border: 1px solid #bbf7d0; border-radius: 8px; padding: 20px; margin: 30px 0; text-align: center;"
          >
            <h4 style="color: #15803d; margin-top: 0;">🎉 Almost Time!</h4>
            <p style="color: #15803d; margin: 0;">
              Your event starts in just
              {{hoursUntilEvent}}
              hours. We're excited for you!
            </p>
          </div>
        {{/if}}
      </div>

      <div class="footer">
        <p><strong>Need help?</strong> Contact us at support@evently.com</p>
        <p>© 2025 Evently. All rights reserved.</p>
        <p style="font-size: 12px; margin-top: 20px;">
          You're receiving this reminder because you have tickets for this
          event.
        </p>
      </div>
    </div>
  </body>
</html>